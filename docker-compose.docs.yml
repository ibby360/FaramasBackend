services:
  docs:
    image: faramas_local_docs
    container_name: faramas_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./faramas:/app/faramas:z
    ports:
      - '9000:9000'
    command: /start-docs
